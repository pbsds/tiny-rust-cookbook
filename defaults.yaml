#highlight-style: pygments
#highlight-style: tango
#highlight-style: espresso
#highlight-style: zenburn
#highlight-style: kate
#highlight-style: monochrome
#highlight-style: breezedark
highlight-style: haddock

#pdf-engine: pdflatex
#pdf-engine: lualatex

filters:
 - pandoc-crossref

variables:
  documentclass: article
  geometry: margin=2.5cm
  papersize: a4
  #TODO: atkinson in HTML output
  header-includes:
      # TODO: include only in latex?
      # see https://ctan.uib.no/macros/latex/contrib/caption/caption-eng.pdf
    - \PassOptionsToPackage{margin=4mm,labelfont=sc}{caption}
      # requires 'texlive-fontsextra' or 'atkinson' from CTAN
    - \usepackage[sfdefault]{atkinson}
